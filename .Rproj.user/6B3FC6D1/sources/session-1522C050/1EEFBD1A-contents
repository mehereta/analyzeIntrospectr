# Edit this in the future
test_that("dataframe with 15 variables created", {
  expect_snapshot_file(...)})
#Informal tests with load_all(): use these commands in console
#statistical.test(final_processed_file,'NOT paired',3,1,1,TRUE)
